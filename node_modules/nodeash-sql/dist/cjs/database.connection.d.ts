export interface DatabaseConfig {
    username: string;
    password: string;
    hostName: string;
    name: string;
}
declare const SQLClient: (DB: DatabaseConfig, logging?: boolean) => {
    DBWrite: any;
    DBRead: any;
    checkDatabaseConnection: () => Promise<boolean>;
};
export default SQLClient;
